plugins {
    id("com.android.library")
    kotlin("android")
}

android {
    namespace = "dev.alucord.plugins"
    compileSdk = 33

    defaultConfig {
        minSdk = 26
    }
}

dependencies {
    compileOnly("com.aliucord:api:2.0.0-beta.1")
}

tasks.register("make") {
    dependsOn("build")
}

